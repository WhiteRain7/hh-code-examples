## Описание

Вырезка из нескольких страниц административной части сайта, с созданием опросов.

## Файлы

- `index.vue` - список опросов и их создание
- `_id/index.vue` - редактирование опроса
- `_id/_link.vue` - заполнение опроса (предпросмотр)